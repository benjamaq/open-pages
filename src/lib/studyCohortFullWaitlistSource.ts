/** Stored on `study_waitlist.source` when signup is from cohort-full capture on the study page. Legacy value kept for existing rows. */
export const STUDY_COHORT_FULL_WAITLIST_SOURCE = 'donotage-suresleep-waitlist'
